# Draft prompts

Draft prompt text is retained here and registered separately in `prompts/prompt-registry.yml`. Drafts are excluded from current-prompt collation and material implementation by default. A draft cannot become approved without an exact recorded human Decision.

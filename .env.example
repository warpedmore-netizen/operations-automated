# Server-side only. Leave unset for local/offline mode.
OPENAI_API_KEY=
# Capability tiers are configured independently so model names can change without code edits.
OPENAI_TIER_1_MODEL=
OPENAI_TIER_2_MODEL=
OPENAI_TIER_3_MODEL=
PORT=4173
